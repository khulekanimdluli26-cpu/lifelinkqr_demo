import {auth,db} from './firebase.js';
import {onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js';
import {doc,getDoc,collection,addDoc,serverTimestamp} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const $=id=>document.getElementById(id),professionalUid=new URLSearchParams(location.search).get('professional');let customer=null;
onAuthStateChanged(auth,async user=>{if(!user){location.href='login.html';return;}const c=await getDoc(doc(db,'customers',user.uid));if(!c.exists()){location.href='customer-register.html';return;}customer={uid:user.uid,...c.data()};if(!professionalUid){$('professional').textContent='No professional selected.';return;}const p=await getDoc(doc(db,'professionalDirectory',professionalUid));if(!p.exists()){$('professional').textContent='Professional not found.';return;}$('professional').innerHTML=`Requesting service from <strong>${esc(p.data().name||'Professional')}</strong>`;$('location').value=customer.serviceArea||'';});
$('requestForm').onsubmit=async e=>{e.preventDefault();if(!customer||!professionalUid)return;const btn=$('submitBtn');btn.disabled=true;$('message').textContent='Sending request...';try{const ref=await addDoc(collection(db,'serviceRequests'),{customerUid:customer.uid,customerName:customer.name||'',professionalName:p.data().name||'Professional',professionalUid,serviceTitle:$('serviceTitle').value.trim(),description:$('description').value.trim(),location:$('location').value.trim(),budget:$('budget').value.trim(),preferredDate:$('preferredDate').value||'',status:'requested',createdAt:serverTimestamp(),updatedAt:serverTimestamp()});$('message').innerHTML=`<div class="success-message">✅ Request sent successfully. Reference: ${esc(ref.id)}</div>`;$('requestForm').reset();$('location').value=customer.serviceArea||'';}catch(err){$('message').textContent='❌ '+err.message;}finally{btn.disabled=false;}};
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
