import {auth,db} from './firebase.js';import {onAuthStateChanged,signOut} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js';import {doc,getDoc,collection,getDocs,query,where} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const $=id=>document.getElementById(id);let uid;
onAuthStateChanged(auth,async u=>{if(!u){location.href='login.html';return;}uid=u.uid;const us=await getDoc(doc(db,'users',uid));if(!us.exists()){location.href='login.html';return;}const d=us.data();$('welcome').textContent=`${d.name||'Professional'} — Business OS`;const snap=await getDocs(query(collection(db,'serviceRequests'),where('professionalUid','==',uid)));let c={requested:0,quoted:0,active:0,completed:0};snap.forEach(x=>{const r=x.data();if(r.professionalUid!==uid)return;if(r.status==='requested')c.requested++;if(r.status==='quoted'||r.status==='accepted')c.quoted++;if(r.status==='in_progress')c.active++;if(r.status==='completed')c.completed++;});$('requests').textContent=c.requested;$('quoted').textContent=c.quoted;$('active').textContent=c.active;$('completed').textContent=c.completed;});$('logoutBtn').onclick=async()=>{await signOut(auth);location.href='login.html';};
